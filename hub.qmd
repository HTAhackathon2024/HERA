---
title: "HTA-Hub"
format:
  html:
    page-layout: full #allows for table to display wider
---

```{r table, echo=FALSE}
library(DT)

# fetch data
pkgs <- read.csv("data/data.csv", stringsAsFactors = FALSE)

# create datatable
datatable(pkgs, colnames = c(
    "Title", "Full package name", "Description", "License", "Authors", "Maintained by","Published", "Has tests", "Has vignettes",
    "Contributors (n)"
)
)
```
