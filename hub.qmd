---
title: "HTA-Hub"
format:
  html:
    page-layout: full #allows for table to display wider
---

```{r table, echo=FALSE}
library(DT)

# fetch data
pkgs <- read.csv("data/dummy_data.csv", stringsAsFactors = FALSE)

# create datatable
datatable(pkgs[-1], colnames = c(
    "", "Name", "Description", "Available on CRAN",
    "Has tests", "Used by", "Contributors (n)",
    "Github rating", "Last commit", "License", "Published",
    "Authors"
)
)
```
